/**
 * Predefined case menu options available for admin selection.
 * Add entries here to make them available in Admin console > Matters > Default menus.
 */
export const CASE_MENU_OPTIONS: string[] = [
  'Events',
  'Finance',
  'Property',
]
